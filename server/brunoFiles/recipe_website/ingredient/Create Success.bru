meta {
  name: Create Success
  type: http
  seq: 2
}

post {
  url: http:/localhost:8080/ingredient
  body: json
  auth: inherit
}

body:json {
  {
    "name": "lettuce",
    "categoryID": "2"
  }
}
